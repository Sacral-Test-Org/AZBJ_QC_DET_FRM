# Spring Datasource Configuration
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=your_username
spring.datasource.password=your_password
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect

# Logging Configuration
logging.level.root=INFO

# Additional Configuration for QC Section
# Add any necessary configuration properties for the QC section here

# Additional Configuration for Dispatch Feature
# Add any necessary configuration properties for the dispatch feature here
